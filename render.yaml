services:
  - type: web
    name: lili-chat
    env: python
    buildCommand: ""
    startCommand: "python chat_lili_web.py"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: MODEL_ID
        sync: false